@model mycourier.Models.Location

@{
    ViewData["Title"] = "Create Location";
    Layout = "/Views/Shared/_LayoutAdmin.cshtml";
}

<div class="container py-5">
    <div id="createCard" class="card shadow-lg border-0 opacity-0 translate-middle-y">
        <div class="card-header text-white d-flex align-items-center"
             style="background:linear-gradient(135deg,#ff8b00 0%,#ffad33 100%);">
            <i class="bi bi-geo-alt fs-3 me-2"></i>
            <h4 class="mb-0 flex-grow-1">Create a New Location</h4>
        </div>
        <div class="card-body bg-light">
            <form asp-action="Create" method="post" class="needs-validation" novalidate>
                <!-- Location Name -->
                <div class="mb-3 input-group">
                    <span class="input-group-text bg-white"><i class="bi bi-geo-alt-fill text-secondary"></i></span>
                    <input asp-for="Name" class="form-control cool-input" placeholder="Enter location name" />
                    <span asp-validation-for="Name" class="text-danger small"></span>
                </div>

                <!-- Location Fee -->
                <div class="mb-3 input-group">
                    <span class="input-group-text bg-white"><i class="bi bi-currency-dollar text-secondary"></i></span>
                    <input asp-for="Fees" class="form-control cool-input" placeholder="Enter fee" />
                    <span asp-validation-for="Fees" class="text-danger small"></span>
                </div>

                <div class="text-end">
                    <button type="submit" class="btn text-white rounded-pill px-5 py-2 shadow-sm btn-hover"
                            style="background: linear-gradient(135deg,#ff8b00 0%,#ffad33 100%);">
                        <i class="bi bi-plus-circle"></i> Create Location
                    </button>
                </div>
            </form>
        </div>
    </div>

    <!-- Location List -->
    <div class="mt-5">
        <h4 class="mb-3 text-dark">Existing Locations</h4>
        <div class="table-responsive shadow-sm rounded">
            <table class="table align-middle mb-0">
                <thead style="background: linear-gradient(135deg,#ff8b00 0%,#ffad33 100%); color: #fff;">
                    <tr>
                        <th><i class="bi bi-geo-alt-fill me-1"></i>Name</th>
                        <th><i class="bi bi-currency-dollar me-1"></i>Fees</th>
                        <th style="width: 180px;"><i class="bi bi-gear-fill me-1"></i>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in ViewBag.LocationsList as List<mycourier.Models.Location>)
                    {
                        <tr>
                            <td>@item.Name</td>
                            <td>@item.Fees.ToString("C")</td>
                            <td>
                                <div class="d-flex gap-2">
                                    <a asp-action="Edit" asp-route-id="@item.Id"
                                       class="btn btn-sm text-white"
                                       style="background: linear-gradient(135deg,#ff8b00 0%,#ffad33 100%);">
                                        <i class="bi bi-pencil-square"></i> Edit
                                    </a>
                                    <form asp-action="DeleteConfirmed" asp-route-id="@item.Id" method="post"
                                          onsubmit="return confirm('Are you sure you want to delete this location?');">
                                        <input type="hidden" name="id" value="@item.Id" />
                                        <button type="submit" class="btn btn-sm btn-outline-danger">
                                            <i class="bi bi-trash"></i> Delete
                                        </button>
                                    </form>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

<style>
    /* Card fade/slide animation */
    .opacity-0 {
        opacity: 0;
    }

    .translate-middle-y {
        transform: translateY(30px);
    }

    #createCard {
        transition: opacity .6s ease, transform .6s ease;
    }

    /* Inputs */
    .cool-input {
        border: none;
        box-shadow: 0 0 0 1px #ccc;
        border-radius: 0.375rem;
        transition: box-shadow .3s ease, transform .2s ease;
    }

        .cool-input:focus {
            box-shadow: 0 0 8px #ff8b00;
            transform: scale(1.02);
        }

    /* Buttons */
    .btn-hover {
        transition: transform .2s ease, box-shadow .2s ease;
    }

        .btn-hover:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(0,0,0,.15);
        }

    /* Table */
    .table-responsive {
        border-radius: 0.5rem;
        overflow: hidden;
        box-shadow: 0 4px 12px rgba(0,0,0,.05);
    }

    .table th, .table td {
        vertical-align: middle;
    }

    .table-hover tbody tr:hover {
        background-color: inherit;
    }

    .btn-sm {
        transition: transform .2s ease, box-shadow .2s ease;
    }

        .btn-sm:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(0,0,0,.15);
        }
</style>

<script>
    document.addEventListener("DOMContentLoaded", function () {
        const card = document.getElementById("createCard");
        setTimeout(() => card.classList.remove("opacity-0", "translate-middle-y"), 50);
    });
</script>
